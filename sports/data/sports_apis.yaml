# data/sports_apis.yaml

- name: TheSportsDB  # Friendly name of the API
  endpoint: https://www.thesportsdb.com/api/v1/json/1/searchplayers.php  # Base URL
  key_name: apikey  # The name of the API key field
  key_value: YOUR_THESPORTSDB_KEY  # Replace with actual key
  query_param: p  # Query string parameter (e.g., p=Messi)
  in_header: false  # If false, key goes in URL params; if true, in headers
  response_format: json  # json or text

- name: API-Football
  endpoint: https://v3.football.api-sports.io/players
  key_name: x-apisports-key
  key_value: YOUR_APIFOOTBALL_KEY
  query_param: search
  in_header: true
  response_format: json

# Add as many APIs as you like. They’ll be tried in order.
